name: QUCI
on: 
  push:
    branches: [ qu_test ]
  pull_request:
    branches: [ qu_test ]
jobs:
  job1:
    runs-on: ubuntu-lastest
    steps:
    - run:
        echo $PATH
        node -v
        npm -v
        python --version
        java --version
        go version
